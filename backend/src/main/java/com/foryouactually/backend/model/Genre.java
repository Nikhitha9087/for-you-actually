package com.foryouactually.backend.model;

/**
 * The five universal genre buckets used across all media.
 * Kept deliberately broad so the same label means the same thing in any language.
 */
public enum Genre {
    THRILLER_MYSTERY,
    HORROR,
    ROMANCE_COMEDY,
    SCIFI_FANTASY,
    DRAMA_SLICE_OF_LIFE
}
